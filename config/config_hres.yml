name: hres_retrieval_v1

model: hres # str, either 'hres' or 'ens'
level: surface # str, only surface is supported for now
retrieval_mode: grid # str, either 'point' or 'grid'
batch_issue: 30 # bool or int, if False, process each issue hour separately; if int, process that many issue day at once

format: grib2 # str, either 'grib2' or 'netcdf'

variables:
  [
    "sd",
    "sf",
    "cp",
    "lsp",
    "msl",
    "10u",
    "10v",
    "2t",
    "2d",
    "tp",
    "100u",
    "100v",
    "z0", # surface roughness
    "sp", # surface pressure
    "blh" # boundary layer height
  ]

issue_hours: ["00", "12"] # list of str, e.g. ["00", "06", "12", "18"] for 4 times a day
lookback: 72 # int, in hours, e.g. 48 means forecasts from the last 48 hours
step_granularity: 1 # int, in hours, e.g. 1 means every hour, 3 means every 3 hours
